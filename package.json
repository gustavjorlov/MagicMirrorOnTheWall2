{
  "name": "MagicMirrorReact",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist && mkdir dist",
    "copy:html": "cp src/index.html dist",
    "copy:icons": "cp -rf src/assets dist",
    "compile:prod": "browserify src/app.js -t babelify | uglifyjs > dist/app.js",
    "compile": "browserify src/app.js -t babelify --debug -o dist/app.js",
    "build": "npm run clean && npm run copy:html && npm run copy:icons && npm run compile",
    "start": "node src/server.js"
  },
  "engines": {
    "node": "4.2.1",
    "npm": "3.7.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "uglify": "^0.1.5"
  },
  "dependencies": {
    "express": "^4.14.0",
    "jquery": "^3.1.1",
    "moment": "^2.15.0",
    "react": "^15.1.0",
    "react-dom": "^15.1.0",
    "request": "^2.75.0",
    "rimraf": "^2.5.4"
  }
}
